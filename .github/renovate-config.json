{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Keep GitHub Actions versions up to date in .github/workflows/ and the Copier template copies under template/.../workflows/, grouped into a single PR with automerge enabled. This file is a self-hosted (global) Renovate config, intentionally named renovate-config.json (not a recognized repo-config filename) so it is not auto-discovered as repository config -- it contains self-hosted-only options such as onboarding, requireConfig and platformCommit.",
  "onboarding": false,
  "requireConfig": "optional",
  "platformCommit": "enabled",
  "enabledManagers": ["github-actions"],
  "github-actions": {
    "managerFilePatterns": [
      "/^\\.github/workflows/[^/]+\\.ya?ml$/",
      "/^template/.*workflows/[^/]+\\.ya?ml$/"
    ]
  },
  "packageRules": [
    {
      "matchManagers": ["github-actions"],
      "groupName": "GitHub Actions",
      "branchPrefix": "chore/",
      "separateMajorMinor": false,
      "automerge": true,
      "automergeType": "pr",
      "automergeStrategy": "squash"
    }
  ]
}
